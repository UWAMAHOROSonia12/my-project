#include<stdio.h>
#include<math.h>
#include<string.h>
int main(){
	int b,a=100;
	     b=45.23;
	printf("the squareroot of a is %f\n",sqrt(a));
		printf("the Sin of a is %f\n",sin(a));
		printf("the Cosine of a is %f\n",cos(a));
		printf("the Cosine of a is %d\n",abs(b));
	return 0;
	}
